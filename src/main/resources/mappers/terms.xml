<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sboard.dao.TermsDAO">

    <select id="select" resultType="kr.co.sboard.dto.TermsDTO">
        SELECT * FROM Terms WHERE no=#{no}
    </select>

    <select id="selectAll" resultType="kr.co.sboard.dto.TermsDTO">
        SELECT * FROM Terms
    </select>

    <insert id="insert">
    </insert>

    <update id="update">
    </update>

    <delete id="delete">
    </delete>
</mapper>